{
  "name": "personal-finance-tracker",
  "version": "1.0.0",
  "description": "A full-stack personal finance tracker application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["finance", "tracker", "react", "nodejs", "mongodb"],
  "author": "chanakya",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
